name: weekly-backup

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Backup placeholder
        run: |
          echo "Create encrypted DB backup artifact here (Supabase or SQLite export)."
          echo "Upload to secure storage (S3/private bucket) in production."
